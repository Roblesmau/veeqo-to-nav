# Netlify config for the static frontend (index.html only).
# We don't need a build step — just publish the repo root.
[build]
  publish = "."

# Friendly URL: https://your-site.netlify.app/  ->  index.html
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200
